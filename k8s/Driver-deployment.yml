apiVersion: apps/v1
kind: Deployment
metadata:
  name: fedcarrier-Driver
  namespace: fedcarrier
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fedcarrier-Driver
  template:
    metadata:
      labels:
        app: fedcarrier-Driver
    spec:
      containers:
        - name: Driver
          image: ghcr.io/fedcarrier/fedcarrier-
          ports:
            - containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Production
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: fedcarrier-secrets
                  key: Driver-connection-string
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: fedcarrier-Driver
  namespace: fedcarrier
spec:
  selector:
    app: fedcarrier-Driver
  ports:
    - port: 500Driver.Length
      targetPort: 8080
  type: ClusterIP
