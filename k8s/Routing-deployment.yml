apiVersion: apps/v1
kind: Deployment
metadata:
  name: fedcarrier-Routing
  namespace: fedcarrier
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fedcarrier-Routing
  template:
    metadata:
      labels:
        app: fedcarrier-Routing
    spec:
      containers:
        - name: Routing
          image: ghcr.io/fedcarrier/fedcarrier-
          ports:
            - containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Production
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: fedcarrier-secrets
                  key: Routing-connection-string
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: fedcarrier-Routing
  namespace: fedcarrier
spec:
  selector:
    app: fedcarrier-Routing
  ports:
    - port: 500Routing.Length
      targetPort: 8080
  type: ClusterIP
